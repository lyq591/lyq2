# 学习笔记：Git与GitHub入门及AI工具调研

> 本文档用于课程设计的代码仓库，记录Git/GitHub学习过程、AI工具选型调研及项目选题规划。

---

## 1. GitHub账号创建

### 1.1 操作步骤

GitHub是一个基于Git的代码托管平台，成立于2008年，2018年被微软收购。创建账号的步骤如下：

1. **访问官网**：在浏览器中打开 https://github.com
2. **点击注册**：选择页面右上角的“Sign up”或“注册”按钮
3. **填写信息**：输入有效的电子邮箱地址、设置密码、选择用户名（建议选择专业、易记的用户名）
4. **验证身份**：完成视觉谜题或音频谜题以验证是人类操作
5. **确认邮箱**：GitHub会向填写的邮箱发送验证码，输入验证码完成邮箱确认

### 1.2 安全建议

注册完成后，建议立即启用**双因素认证（2FA）**以增强账户安全性。开启后，每次登录除密码外还需输入手机验证器App生成的动态验证码，即使密码泄露也能有效保护账户。

GitHub免费账户支持创建**无限数量的公开和私有仓库**，可使用GitHub Copilot AI辅助编程、GitHub Actions自动化工作流等功能。

---

## 2. GitHub代码仓库创建

### 2.1 创建新仓库

登录GitHub后，按以下步骤创建课程设计的代码仓库：

1. **点击“+”号**：在页面右上角点击“+”图标，选择“New repository”
2. **填写仓库信息**：
   - Repository name：输入项目名称（如 `course-design`）
   - Description：可填写简短的项目描述
   - 选择 **Public** 或 **Private**（课程设计可设为Private）
3. **初始化选项**：勾选“Add a README file”以便仓库创建后包含说明文档
4. **点击创建**：点击“Create repository”完成

### 2.2 本地推送流程

若本地已有项目文件，可通过以下步骤将代码推送到GitHub：

```bash
# 1. 进入项目目录
cd /path/to/your/project

# 2. 初始化本地Git仓库
git init

# 3. 添加所有文件到暂存区
git add .

# 4. 首次提交前配置用户信息（仅需一次）
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"

# 5. 提交到本地仓库
git commit -m "Initial commit: 课程设计项目初始化"

# 6. 将本地默认分支重命名为main
git branch -M main

# 7. 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/仓库名.git

# 8. 首次推送
git push -u origin main
