# 柔性作业车间智能调度辅助Agent系统 — 系统设计报告

| 项目名称 | 基于DeepSeek Harness的柔性作业车间智能调度辅助Agent |
|----------|------------------------------------------------------|
| 文档版本 | V1.0 |
| 编写日期 | 2026年9月 |
| 学生 | 李勇琦 |
| 指导教师 | 董绪斌 |
| 课程 | 智能系统设计与集成 / 制造智能技术 |

---

## 1. 系统概述

### 1.1 设计目标

本系统旨在构建一个 B/S 架构的柔性作业车间智能调度辅助平台，核心目标包括：

1. **智能求解**：基于遗传算法自动求解含工装约束的 FJSP 调度问题
2. **可视交互**：提供甘特图、收敛曲线、数字孪生等多维度可视化
3. **数据驱动**：集成工业大数据分析，支持瓶颈识别与多实例对比
4. **决策透明**：以自然语言解释调度决策，辅助管理人员理解方案
5. **轻量易用**：纯Python后端 + CDN前端，无需复杂部署即可运行

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| 模块化 | 算法、数据、API、前端各层独立，低耦合高内聚 |
| 纯Python | 后端不依赖含原生DLL的库，确保在受限环境可运行 |
| 可测试 | 核心模块均有自动化单元测试 |
| 可扩展 | 算法可替换、数据源可扩展、前端组件可复用 |
| 用户友好 | 操作流程简洁，错误提示明确，加载状态可见 |

---

## 2. 系统架构设计

### 2.1 总体架构

系统采用经典的三层 B/S 架构：

```
┌─────────────────────────────────────────────────────────┐
│                     表现层（前端）                          │
│  Vue 3 响应式框架 + ECharts 5 可视化库                    │
│  页面：概览/数据集/调度/分析/数字孪生/历史                  │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTP/RESTful API (JSON)
┌──────────────────────────▼──────────────────────────────┐
│                     业务逻辑层（后端）                       │
│  Flask Web框架                                              │
│  ┌────────────┐ ┌────────────┐ ┌──────────────────────┐ │
│  │  调度算法模块 │ │ 数据分析模块 │ │    API路由层         │ │
│  │  (GA求解器)  │ │ (统计/瓶颈) │ │ (请求处理/参数校验)  │ │
│  └────────────┘ └────────────┘ └──────────────────────┘ │
└──────────────────────────┬──────────────────────────────┘
                           │ SQL
┌──────────────────────────▼──────────────────────────────┐
│                     数据层（数据库）                         │
│  SQLite 轻量级数据库                                        │
│  表：调度任务 / 调度结果 / 数据集缓存 / 系统日志             │
│  + 文件存储：data/processed/*.json（实例数据）              │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 前端框架 | Vue 3 (CDN) | 轻量响应式，无需构建工具，CDN直接引入 |
| 可视化库 | ECharts 5 (CDN) | 功能强大，支持甘特图/折线图/柱状图，中文文档完善 |
| 后端框架 | Flask 3.x | 纯Python实现，不依赖pydantic原生库，轻量灵活 |
| 跨域处理 | flask-cors | 解决前后端分离的跨域问题 |
| 数据库 | SQLite 3 | Python内置，零配置，文件级存储，适合课程设计 |
| 调度算法 | 自研遗传算法 | 纯Python实现，POX交叉，可完全控制编码与解码逻辑 |
| 版本控制 | Git + GitHub | 代码托管，提交记录可追溯 |

**选型说明**：原计划采用 FastAPI + pydantic 技术栈，但因系统应用控制策略阻止 pydantic_core 原生 DLL 加载，改为纯 Python 的 Flask 框架。算法层同样避免使用 numpy，全部采用 Python 原生数据结构（list/dict）实现。

### 2.3 部署架构

```
用户浏览器 (Chrome/Edge)
    │
    ├── http://127.0.0.1:5500 ──→ Python HTTP Server (前端静态文件)
    │
    └── http://127.0.0.1:8000 ──→ Flask 后端服务
                                       ├── 算法计算 (CPU)
                                       ├── SQLite 数据库 (文件IO)
                                       └── 实例数据读取 (JSON文件)
```

---

## 3. 数据库设计

### 3.1 ER图

```
┌──────────────────┐       ┌──────────────────────┐
│  schedule_tasks   │       │  schedule_results     │
│──────────────────│       │──────────────────────│
│ id (PK)          │──1:1─→│ id (PK)              │
│ instance_id      │       │ task_id (FK)          │
│ instance_name    │       │ instance_id            │
│ parameters (JSON)│       │ makespan               │
│ status           │       │ machine_schedule (JSON)│
│ result (JSON)    │       │ job_schedule (JSON)    │
│ makespan         │       │ machine_utilization    │
│ elapsed_time     │       │ convergence_history    │
│ created_at       │       │ analysis (JSON)        │
│ updated_at       │       │ created_at             │
└──────────────────┘       └──────────────────────┘

┌──────────────────┐       ┌──────────────────────┐
│  dataset_cache    │       │  system_logs          │
│──────────────────│       │──────────────────────│
│ instance_id (PK) │       │ id (PK)              │
│ data (JSON)      │       │ level                 │
│ stats (JSON)     │       │ module                │
│ cached_at        │       │ message               │
└──────────────────┘       │ created_at            │
                            └──────────────────────┘
```

### 3.2 表结构详细设计

#### 3.2.1 schedule_tasks（调度任务表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 任务唯一标识 |
| instance_id | TEXT | NOT NULL | 实例ID，如"FJSP-F1" |
| instance_name | TEXT | | 实例名称 |
| parameters | TEXT | | 算法参数，JSON格式 |
| status | TEXT | DEFAULT 'pending' | 任务状态：pending/running/completed/failed |
| result | TEXT | | 结果摘要，JSON格式 |
| makespan | REAL | | 求解得到的总工期 |
| elapsed_time | REAL | | 求解耗时（秒） |
| created_at | REAL | DEFAULT (strftime('%s','now')) | 创建时间戳 |
| updated_at | REAL | | 更新时间戳 |

#### 3.2.2 schedule_results（调度结果表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 结果唯一标识 |
| task_id | INTEGER | NOT NULL | 关联任务ID |
| instance_id | TEXT | NOT NULL | 实例ID |
| makespan | REAL | NOT NULL | 总工期 |
| machine_schedule | TEXT | NOT NULL | 机器调度表，JSON数组 |
| job_schedule | TEXT | NOT NULL | 作业调度表，JSON数组 |
| machine_utilization | TEXT | | 机器利用率，JSON数组 |
| convergence_history | TEXT | | 收敛历史，JSON数组 |
| analysis | TEXT | | 分析与解释，JSON对象 |
| created_at | REAL | DEFAULT (strftime('%s','now')) | 创建时间戳 |

#### 3.2.3 dataset_cache（数据集缓存表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| instance_id | TEXT | PRIMARY KEY | 实例ID |
| data | TEXT | NOT NULL | 实例完整数据，JSON |
| stats | TEXT | | 实例统计信息，JSON |
| cached_at | REAL | DEFAULT (strftime('%s','now')) | 缓存时间 |

**设计说明**：缓存表用于避免重复读取和解析JSON文件，提升API响应速度。首次访问实例时从文件加载并写入缓存，后续直接从缓存读取。

#### 3.2.4 system_logs（系统日志表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 日志ID |
| level | TEXT | NOT NULL | 级别：info/warning/error |
| module | TEXT | | 产生日志的模块名 |
| message | TEXT | NOT NULL | 日志内容 |
| created_at | REAL | DEFAULT (strftime('%s','now')) | 时间戳 |

### 3.3 数据库访问层设计

采用 `Database` 类封装所有数据库操作，使用 Python 内置 `sqlite3` 模块。关键设计：

1. **单例模式**：通过 `get_db()` 全局函数获取唯一数据库实例
2. **自动建表**：初始化时执行 `CREATE TABLE IF NOT EXISTS`
3. **JSON序列化**：复杂数据（调度表、参数等）以JSON字符串存储，读取时自动反序列化
4. **参数化查询**：所有SQL使用参数化查询，防止SQL注入
5. **连接管理**：每个操作获取连接、执行、关闭，避免长连接占用

---

## 4. API接口设计

### 4.1 接口规范

- **协议**：HTTP/1.1
- **基础URL**：`http://127.0.0.1:8000/api`
- **数据格式**：请求与响应均为 JSON（Content-Type: application/json）
- **字符编码**：UTF-8
- **跨域**：CORS 全开（Access-Control-Allow-Origin: *）

### 4.2 统一响应格式

**成功响应**：直接返回数据对象或数组
```json
{ "status": "healthy", "timestamp": 1788409058.76 }
```

**错误响应**：
```json
{ "detail": "实例 FJSP-F99 不存在" }
```
HTTP状态码：400（参数错误）、404（资源不存在）、500（服务器内部错误）

### 4.3 接口详细设计

#### 4.3.1 健康检查

```
GET /api/health
```

**响应**：
```json
{
  "status": "healthy",
  "timestamp": 1788409058.76,
  "database_stats": {
    "total_tasks": 5,
    "total_results": 5,
    "cached_instances": 20,
    "avg_makespan": 29.5
  }
}
```

#### 4.3.2 获取实例列表

```
GET /api/dataset/instances
```

**响应**：
```json
{
  "instances": [
    {
      "instance_id": "FJSP-F1",
      "num_jobs": 3,
      "num_machines": 2,
      "num_fixtures": 2,
      "operations_per_job": 3,
      "total_processing_time": 73
    }
  ],
  "total": 20
}
```

#### 4.3.3 获取实例详情

```
GET /api/dataset/{instance_id}
```

**响应**：完整的实例JSON数据（含scale、jobs、metadata）

#### 4.3.4 获取实例统计

```
GET /api/dataset/{instance_id}/stats
```

**响应**：
```json
{
  "instance_id": "FJSP-F1",
  "total_operations": 9,
  "avg_operations_per_job": 3.0,
  "processing_time_stats": { "mean": 5.62, "min": 2, "max": 12, "std": 2.85 },
  "machine_workload": [{"machine_id": 1, "eligible_op_count": 7}],
  "fixture_usage": [{"fixture_id": 1, "eligible_op_count": 5}],
  "delivery_time_stats": { "mean": 50.0, "min": 40, "max": 60 },
  "priority_distribution": { "1": 3 }
}
```

#### 4.3.5 瓶颈识别

```
GET /api/dataset/{instance_id}/bottlenecks
```

**响应**：
```json
{
  "instance_id": "FJSP-F1",
  "machine_bottlenecks": [
    {"machine_id": 2, "eligible_op_count": 8, "is_bottleneck": true}
  ],
  "avg_machine_load": 7.5,
  "fixture_bottlenecks": [{"fixture_id": 1, "eligible_op_count": 6}],
  "recommendation": "建议优先关注高负荷机器..."
}
```

#### 4.3.6 执行调度求解

```
POST /api/schedule
Content-Type: application/json

{
  "instance_id": "FJSP-F1",
  "pop_size": 100,
  "max_gen": 200,
  "seed": 42
}
```

**响应**：
```json
{
  "task_id": 6,
  "result_id": 6,
  "makespan": 28,
  "elapsed_time": 0.33,
  "machine_schedule": [
    [{"job_id": 1, "operation_id": 1, "machine_id": 1, "start_time": 0, "end_time": 5}]
  ],
  "job_schedule": [[...]],
  "machine_utilization": [{"machine_id": 1, "utilization_rate": 96.4, "num_tasks": 5}],
  "convergence_history": [{"generation": 1, "best_makespan": 35, "avg_makespan": 42.5}],
  "analysis": {
    "makespan": 28,
    "critical_path_jobs": [{"job_id": 3, "finish_time": 28}],
    "machine_load_balance": {"avg_utilization": 96.4, "utilization_std": 0.0, "is_balanced": true},
    "explanation": "本次调度方案的总工期..."
  }
}
```

#### 4.3.7 多实例对比

```
GET /api/analysis/compare?instances=FJSP-F1,FJSP-F10,FJSP-F20
```

**响应**：
```json
{
  "comparison": [
    {"instance_id": "FJSP-F1", "num_jobs": 3, "num_machines": 2, "total_operations": 9, "avg_processing_time": 5.62}
  ]
}
```

#### 4.3.8 调度历史

```
GET /api/schedule/results?limit=20
```

**响应**：
```json
{
  "tasks": [{"id": 6, "instance_id": "FJSP-F1", "status": "completed", "makespan": 28, "elapsed_time": 0.33}],
  "total": 6
}
```

---

## 5. 遗传算法设计

### 5.1 算法概述

本系统采用遗传算法（Genetic Algorithm, GA）求解含工装约束的柔性作业车间调度问题。算法核心是通过模拟自然选择与遗传进化，在解空间中搜索近优调度方案。

### 5.2 编码方案

采用**两段式染色体编码**：

#### 5.2.1 操作序列段（OS）

- 每个作业号 `j` 在序列中出现 `n_j` 次（`n_j` 为作业j的工序数）
- 第 k 次出现的作业号 j 表示作业 j 的第 k 道工序
- 序列长度 = 总工序数 Σn_j
- 示例（3作业，每作业3工序）：`[1, 2, 1, 3, 2, 3, 1, 2, 3]`

#### 5.2.2 机器分配段（MA）

- 每个元素为对应工序分配的机器编号
- 长度 = 总工序数
- 机器编号必须在该工序的可选机器集合中
- 示例：`[1, 2, 1, 2, 1, 2, 1, 2, 1]`

### 5.3 解码算法

采用**主动调度解码（Active Schedule Decoding）**：

```
输入：操作序列OS，机器分配MA
输出：机器调度表，作业调度表，Makespan

1. 初始化：
   - machine_available_time[m] = 0  (每台机器的可用时间)
   - job_next_op[j] = 0  (每个作业的下一道工序索引)
   - fixture_available_time[f] = 0  (每套工装的可用时间)

2. 遍历操作序列中的每个作业号j：
   a. op_idx = job_next_op[j]，获取工序详情
   b. machine = MA中对应位置的机器编号
   c. processing_time = 该工序在machine上的加工时间
   d. fixture = 选择可用的工装（eligible_fixtures中最早可用的）
   e. start_time = max(machine_available_time[machine],
                        job_last_finish_time[j],
                        fixture_available_time[fixture])
   f. end_time = start_time + processing_time
   g. 更新：machine_available_time[machine] = end_time
            job_last_finish_time[j] = end_time
            fixture_available_time[fixture] = end_time
   h. 记录调度信息到机器调度表和作业调度表
   i. job_next_op[j] += 1

3. Makespan = max(job_last_finish_time)
```

### 5.4 适应度函数

```
fitness(chromosome) = 1 / Makespan(chromosome)
```

Makespan 越小，适应度越高。

### 5.5 遗传操作

#### 5.5.1 选择操作

采用**锦标赛选择（Tournament Selection）**：
- 锦标赛大小 k = 3
- 从种群中随机选取 k 个个体，选择适应度最高的作为父代
- 重复直到选满种群大小

#### 5.5.2 交叉操作

**操作序列段**：采用 **POX（Precedence Operation Crossover）交叉**

```
1. 将作业集随机分为两个非空子集 J1 和 J2
2. 子代1：
   - 从父代1复制J1中作业的所有出现位置（保持原顺序）
   - 从父代2复制J2中作业的所有出现位置（按父代2顺序填入空位）
3. 子代2：
   - 从父代2复制J1中作业的所有出现位置
   - 从父代1复制J2中作业的所有出现位置
```

POX交叉的优势：保持每个作业内部工序的先后顺序，生成的子代始终是合法调度。

**机器分配段**：采用**均匀交叉（Uniform Crossover）**
- 每个位置以50%概率从父代1继承，50%从父代2继承
- 交叉后校验机器合法性，不合法则随机重选

#### 5.5.3 变异操作

**操作序列段**：**交换变异（Swap Mutation）**
- 随机选择两个位置，交换其元素
- 变异概率 Pm = 0.1

**机器分配段**：**随机重选变异**
- 随机选择一个位置，从该工序的可选机器中随机选择另一台
- 变异概率 Pm = 0.1

#### 5.5.4 精英保留

- 每代保留当前最优个体（Elite）直接进入下一代
- 精英数量 = 1
- 确保最优解不会因交叉变异而丢失

### 5.6 算法流程

```
开始
  │
  ▼
初始化种群（随机生成 pop_size 个染色体）
  │
  ▼
评估种群（解码每个染色体，计算Makespan和适应度）
  │
  ▼
记录最优个体，记录收敛数据
  │
  ▼
┌─→ 达到最大迭代代数？──是──→ 输出最优解，结束
│        │
│        否
│        ▼
│   选择（锦标赛选择）
│        │
│        ▼
│   交叉（POX + 均匀交叉）
│        │
│        ▼
│   变异（交换 + 随机重选）
│        │
│        ▼
│   精英保留（最优个体直接进入下一代）
│        │
│        ▼
│   评估新种群
│        │
│        ▼
└─── 更新最优个体，记录收敛数据
```

### 5.7 参数设置

| 参数 | 默认值 | 范围 | 说明 |
|------|--------|------|------|
| pop_size | 100 | 10-500 | 种群大小 |
| max_gen | 200 | 10-1000 | 最大迭代代数 |
| seed | 42 | 任意整数 | 随机种子（保证可复现） |
| crossover_rate | 0.8 | - | 交叉概率 |
| mutation_rate | 0.1 | - | 变异概率 |
| tournament_size | 3 | - | 锦标赛大小 |
| elite_count | 1 | - | 精英保留数量 |

### 5.8 算法性能

| 实例 | 规模 | Makespan | 求解时间 | 机器利用率 |
|------|------|----------|----------|------------|
| FJSP-F1 | 3作业×2机器 | 28 | 0.33s | 96.4% |
| FJSP-F10 | 8作业×6机器 | - | <5s | - |
| FJSP-F20 | 20作业×10机器 | - | <30s | - |

---

## 6. 前端设计

### 6.1 前端架构

采用 **Vue 3 Composition API** 构建单页应用（SPA），通过 CDN 引入，无需 Node.js 构建环境。

```
index.html (入口页面)
├── <head>
│   ├── Vue 3 CDN
│   ├── ECharts 5 CDN
│   └── css/style.css
├── <body>
│   └── <div id="app">
│       ├── 顶部导航栏
│       ├── 侧边栏菜单
│       └── 主内容区（7个页面组件）
└── <script src="js/app.js">
```

### 6.2 页面设计

| 页面 | 路由标识 | 功能 |
|------|----------|------|
| 系统概览 | dashboard | 数据集统计卡片、数据库状态、API连接状态 |
| 数据集管理 | dataset | 实例列表表格、实例详情弹窗、跳转调度 |
| 智能调度 | schedule | 实例选择、参数配置、调度执行、甘特图、收敛曲线、利用率、决策解释 |
| 数据分析 | analysis | 实例选择、统计信息、机器负荷图、瓶颈分析、规模分布图、多实例对比 |
| 数字孪生 | twin | 车间布局、机器状态动画、作业状态、时间轴控制 |
| 调度历史 | history | 历史调度记录列表、结果查看 |

### 6.3 核心组件设计

#### 6.3.1 甘特图组件

基于 ECharts custom series 实现：
- 横轴：时间
- 纵轴：机器（category）
- 每个工序渲染为矩形色块
- 颜色按作业ID区分（20种预设颜色循环）
- 色块内显示"Jx-Oy"标签
- 鼠标悬停显示工序详情（作业、工序、机器、开始/结束/时长）

#### 6.3.2 收敛曲线组件

基于 ECharts line series：
- 双曲线：最优Makespan + 平均Makespan
- 横轴：迭代代数
- 纵轴：Makespan值
- 平滑曲线，数据点可交互

#### 6.3.3 数字孪生组件

基于 DOM + CSS 动画实现：
- 车间布局：机器卡片网格 + 作业状态列表
- 机器状态：idle（灰色）/ running（绿色，显示进度条）
- 作业状态：waiting（黄色）/ running（蓝色）/ done（绿色）
- 时间控制：播放/暂停按钮，当前时间显示
- 动画逻辑：setInterval 每200ms推进1个时间单位，根据调度结果更新机器和作业状态

### 6.4 状态管理

使用 Vue 3 `ref` 和 `reactive` 管理应用状态：

```javascript
// 核心状态
const currentPage = ref('dashboard')      // 当前页面
const apiConnected = ref(false)            // API连接状态
const instances = ref([])                   // 实例列表
const scheduleResult = ref(null)            // 当前调度结果
const scheduleConfig = reactive({...})      // 调度参数配置
const selectedInstance = ref({})            // 选中的实例
const instanceStats = ref(null)             // 实例统计
const bottleneckAnalysis = ref(null)        // 瓶颈分析
const history = ref([])                      // 调度历史
const datasetOverview = ref({})             // 数据集概览
const twinMachines = ref([])                 // 数字孪生机器状态
const twinJobs = ref([])                     // 数字孪生作业状态
```

### 6.5 API封装

```javascript
const API_BASE = 'http://127.0.0.1:8000/api';

async function apiGet(path) {
    const res = await fetch(API_BASE + path);
    return res.ok ? await res.json() : null;
}

async function apiPost(path, data) {
    const res = await fetch(API_BASE + path, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
    });
    if (!res.ok) throw new Error((await res.json()).detail);
    return await res.json();
}
```

---

## 7. 模块详细设计

### 7.1 后端模块结构

```
backend/
├── main.py              # Flask应用入口，API路由
├── database.py          # SQLite数据库访问层
├── requirements.txt     # Python依赖
└── algorithms/
    ├── __init__.py      # 模块导出
    ├── fjsp_ga.py       # FJSP遗传算法求解器
    └── analysis.py      # 工业数据分析器
```

### 7.2 FJSPSolver 类设计

```python
class FJSPSolver:
    """FJSP遗传算法求解器"""

    def __init__(self, jobs_data, num_machines, num_fixtures=0,
                 pop_size=100, max_gen=200, crossover_rate=0.8,
                 mutation_rate=0.1, seed=None):
        # 初始化参数，解析作业数据，构建操作信息表

    def solve(self):
        """执行遗传算法求解，返回调度结果字典"""

    def decode(self, chromosome):
        """解码染色体，返回(makespan, machine_schedule, job_schedule)"""

    def _random_chromosome(self):
        """生成随机染色体"""

    def _tournament_select(self, population):
        """锦标赛选择"""

    def _pox_crossover(self, parent1, parent2):
        """POX交叉操作序列"""

    def _uniform_crossover_ma(self, parent1, parent2):
        """均匀交叉机器分配"""

    def _mutate(self, chromosome):
        """变异操作"""

    def _calculate_machine_utilization(self, machine_schedule, makespan):
        """计算机器利用率"""
```

### 7.3 DataAnalyzer 类设计

```python
class DataAnalyzer:
    """工业数据分析器"""

    def __init__(self, data_dir):
        # 加载所有实例数据

    def get_instance_stats(self, instance_id):
        """获取实例统计信息（工序、加工时间、机器负荷、工装使用、交货期）"""

    def identify_bottlenecks(self, instance_id):
        """识别瓶颈机器和工装"""

    def compare_instances(self, instance_ids):
        """多实例横向对比"""

    def get_dataset_overview(self):
        """数据集整体概览"""

    def analyze_schedule_result(self, schedule_result, instance_data):
        """分析调度结果，生成决策解释"""

    def _generate_explanation(self, ...):
        """生成自然语言解释文本"""
```

### 7.4 Database 类设计

```python
class Database:
    """SQLite数据库访问层"""

    def __init__(self, db_path):
        # 连接数据库，创建表

    def create_task(self, instance_id, instance_name, parameters):
        """创建调度任务，返回task_id"""

    def update_task_status(self, task_id, status, result=None, makespan=None, elapsed_time=None):
        """更新任务状态"""

    def get_task(self, task_id):
        """获取任务详情"""

    def list_tasks(self, limit=20):
        """获取任务列表"""

    def save_result(self, task_id, instance_id, schedule_result, analysis):
        """保存调度结果，返回result_id"""

    def get_result(self, result_id):
        """获取结果详情"""

    def get_latest_result(self, instance_id):
        """获取某实例最新结果"""

    def cache_instance(self, instance_id, data, stats):
        """缓存实例数据"""

    def get_cached_instance(self, instance_id):
        """获取缓存的实例数据"""

    def add_log(self, level, module, message):
        """添加系统日志"""

    def get_logs(self, limit=50, level=None):
        """获取日志列表"""

    def get_stats(self):
        """获取数据库统计信息"""
```

---

## 8. 测试设计

### 8.1 测试策略

采用**分层测试**策略，覆盖从算法单元到API接口的各个层级：

| 测试层级 | 测试对象 | 测试数量 | 工具 |
|----------|----------|----------|------|
| 单元测试 | 遗传算法 | 5项 | Python assert |
| 单元测试 | 数据库模块 | 5项 | Python assert |
| 单元测试 | 数据分析模块 | 6项 | Python assert |
| 集成测试 | API接口 | 12项 | Flask test_client |
| 端到端测试 | 完整用户流程 | 8步 | HTTP请求模拟 |
| **合计** | | **28项 + E2E** | |

### 8.2 测试用例设计

#### 8.2.1 遗传算法测试（test_ga.py）

| 编号 | 测试项 | 验证内容 |
|------|--------|----------|
| GA-1 | 求解器初始化 | 作业数、机器数、总工序数、操作信息表正确 |
| GA-2 | 染色体生成 | 操作序列长度、机器分配长度、作业出现次数正确 |
| GA-3 | 解码功能 | Makespan>0、调度任务数=总工序数、机器/作业调度表长度正确 |
| GA-4 | 完整求解 | 返回字段完整、Makespan>0、收敛历史长度=迭代代数、最优值单调不增 |
| GA-5 | 机器利用率 | 利用率列表长度=机器数、利用率在0-100%之间 |

#### 8.2.2 数据库测试（test_database.py）

| 编号 | 测试项 | 验证内容 |
|------|--------|----------|
| DB-1 | 数据库初始化 | 文件创建、统计表字段完整 |
| DB-2 | 任务管理 | 创建、获取、状态更新、列表查询 |
| DB-3 | 结果管理 | 保存、获取、JSON反序列化、最新结果查询 |
| DB-4 | 数据集缓存 | 缓存写入、读取、数据一致性 |
| DB-5 | 系统日志 | 日志写入、按级别筛选 |

#### 8.2.3 数据分析测试（test_analysis.py）

| 编号 | 测试项 | 验证内容 |
|------|--------|----------|
| AN-1 | 分析器初始化 | 实例加载数量、关键实例存在 |
| AN-2 | 实例统计 | 总工序数>0、平均加工时间>0、字段完整 |
| AN-3 | 瓶颈识别 | 机器瓶颈列表非空、建议文本非空 |
| AN-4 | 数据集概览 | 实例数=20、规模范围合理、实例列表完整 |
| AN-5 | 实例对比 | 对比数量正确、规模数据递增 |
| AN-6 | 调度结果分析 | 解释文本>50字、包含关键信息（关键路径/工期） |

#### 8.2.4 API接口测试（test_api.py）

| 编号 | 测试项 | 验证内容 |
|------|--------|----------|
| API-1 | 健康检查 | 状态200、status=healthy、含数据库统计 |
| API-2 | 根路径 | 状态200、含name/version |
| API-3 | 实例列表 | 状态200、total=20、实例结构完整 |
| API-4 | 实例详情 | 状态200、instance_id正确、含scale/jobs/metadata |
| API-5 | 不存在实例 | 状态404 |
| API-6 | 实例统计 | 状态200、含total_operations/processing_time_stats |
| API-7 | 瓶颈识别 | 状态200、含machine_bottlenecks/recommendation |
| API-8 | 数据集概览 | 状态200、num_instances=20 |
| API-9 | 调度求解 | 状态200、Makespan>0、含machine_schedule/analysis |
| API-10 | 调度不存在实例 | 状态404 |
| API-11 | 调度历史 | 状态200、含tasks/total |
| API-12 | 实例对比 | 状态200、comparison长度=3 |

### 8.3 测试运行

```bash
# 运行全部测试
python tests/run_tests.py

# 单独运行某模块测试
python tests/test_ga.py
python tests/test_database.py
python tests/test_analysis.py
python tests/test_api.py
```

### 8.4 测试结果

全部 28 项自动化测试通过，端到端流程测试通过。

---

## 9. 关键技术难点与解决方案

### 9.1 原生库DLL加载被阻止

**问题**：系统应用控制策略阻止 numpy、pydantic_core 等含原生DLL的库加载，导致 FastAPI、numpy 等常用库无法使用。

**解决方案**：
1. 后端框架从 FastAPI 改为纯 Python 的 Flask
2. 遗传算法全部使用 Python 原生 list/dict 实现，不依赖 numpy
3. 数据统计使用 Python 内置 math 模块计算均值、标准差
4. JSON 序列化/反序列化使用 Python 内置 json 模块

### 9.2 MATLAB .mat 文件解析

**问题**：FJSP-F 数据集为 MATLAB .mat 格式，scipy.io.loadmat 依赖 numpy 无法使用；.mat 格式复杂（含压缩元素、小数据元素、无8字节填充等特殊格式）。

**解决方案**：
1. 编写纯 Python .mat 解析器（`data/scripts/mat_reader.py`），仅使用 struct + zlib
2. 逐步调试，识别该数据集的特殊格式特征
3. 对解析不完整的字段，按 FJSP 特征 + 固定种子补全合理值（用户明确指示）

### 9.3 Git HTTPS传输失败

**问题**：Git HTTPS 传输依赖的 libcurl-4.dll 被系统应用控制策略阻止加载，重装无效。

**解决方案**：
1. 改用 SSH 协议，使用 Windows 自带 OpenSSH 生成 ed25519 密钥
2. 22端口被网络拒绝，配置 `~/.ssh/config` 走 `ssh.github.com:443`
3. 成功推送所有提交

### 9.4 遗传算法编码与解码

**问题**：FJSP 的染色体编码需要同时表示工序优先级和机器分配，解码时需满足工艺约束、机器约束、工装约束。

**解决方案**：
1. 采用两段式编码（操作序列 + 机器分配）
2. 操作序列使用基于作业号的编码，天然满足工序先后约束
3. POX交叉保序，确保子代始终合法
4. 解码时同时跟踪机器可用时间、作业上道工序完成时间、工装可用时间，取最大值作为开始时间

---

## 10. 系统特色与创新点

### 10.1 纯Python轻量架构

后端不依赖任何含原生DLL的第三方库，全部使用 Python 标准库 + Flask（纯Python），在受限环境下仍可完整运行。

### 10.2 调度决策透明化

不仅给出调度方案，还通过自然语言解释决策依据：关键路径识别、机器负载均衡分析、交货期满足情况、优化建议，使调度结果可理解、可追溯。

### 10.3 数字孪生车间可视化

基于调度结果生成车间运行状态动画，实时映射每台机器的加工状态和每个作业的进度，使抽象的调度方案变得直观可见。

### 10.4 多技术方向融合

系统融合了四个智能制造技术方向：
1. **工艺规划与车间调度**：遗传算法求解FJSP
2. **工业大数据采集与分析**：数据集统计、瓶颈识别、多实例对比
3. **数字孪生与系统仿真**：车间状态映射与动画
4. **智能制造系统集成**：RESTful API、SQLite数据库、任务管理、系统日志

---

## 11. 部署与运行说明

### 11.1 环境准备

```bash
# 安装Python依赖
cd backend
pip install -r requirements.txt
```

### 11.2 启动方式

**方式一：一键启动（推荐）**
```bash
scripts\start_all.bat
```

**方式二：分别启动**
```bash
# 终端1：启动后端
cd backend
python main.py

# 终端2：启动前端
cd frontend
python -m http.server 5500
```

### 11.3 访问地址

- 前端界面：http://127.0.0.1:5500
- 后端API：http://127.0.0.1:8000
- 健康检查：http://127.0.0.1:8000/api/health

### 11.4 运行测试

```bash
python tests\run_tests.py
```

---

## 12. 总结

本系统完成了柔性作业车间智能调度辅助Agent的完整设计与实现，包括：

1. **后端服务**：Flask RESTful API，20+接口，SQLite数据库，纯Python实现
2. **调度算法**：自研遗传算法，POX交叉，锦标赛选择，精英保留，含工装约束
3. **数据分析**：实例统计、瓶颈识别、多实例对比、数据集概览、决策解释
4. **前端界面**：Vue3 + ECharts单页应用，7个功能页面，甘特图/收敛曲线/数字孪生
5. **自动化测试**：4个测试套件28项测试全部通过
6. **文档齐全**：需求规格说明书、系统设计报告、README、API文档

系统在受限环境（原生DLL被阻止）下仍能完整运行，体现了良好的工程适应性。后续可扩展方向包括：更多调度算法（模拟退火、强化学习）、多目标优化（工期+成本+能耗）、实时调度与重调度、与MES系统集成等。

---

*文档结束*
